# demo env
# this redis container is inside the same docker network where reverse proxy is so the address looks like that , the name of the redis container is "redis"
REDIS_ADDRESS = "redis:6379"
REDIS_PASSWORD = ""
